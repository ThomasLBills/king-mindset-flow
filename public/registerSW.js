// Intentionally empty. The old build loaded this file to register its
// service worker. It now does nothing; public/sw.js carries the kill-switch
// that removes any worker the old build left behind.
