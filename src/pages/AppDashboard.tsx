import Index from "./Index";

// The main app dashboard - reuses the existing Index page
// This is the entitlement-gated version
const AppDashboard = () => {
  return <Index />;
};

export default AppDashboard;
